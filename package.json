{
  "name": "signal-radio",
  "private": true,
  "description": "Dev-only tooling to compile tailwind.css. Not needed to deploy the site -- the compiled output is committed.",
  "scripts": {
    "build:css": "tailwindcss -i ./tailwind.input.css -o ./tailwind.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "3.4.17"
  }
}
